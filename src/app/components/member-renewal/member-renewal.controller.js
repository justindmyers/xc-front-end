class MemberRenewalController {
    constructor($scope, $window) {
        'ngInject';
        this.$scope = $scope;
        this.$window = $window;

        this.test = '83.68%';
    }
}

export default MemberRenewalController;
